apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: "{{ .Release.Namespace }}"
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
  - port: {{ .Values.ports.redis }}
    targetPort: {{ .Values.ports.redis }}
    protocol: TCP
    name: redis